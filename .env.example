# PrivacyCompliance Example Environment Variables
#
# INSTRUCTIONS:
# 1. Copy this file to one of the following, depending on your environment:
#    - .env.development  (for local development)
#    - .env.production   (for production deployment)
#    - .env.test         (for running tests)
# 2. Fill in the values for each variable as appropriate for your setup.
# 3. NEVER commit your real .env files to version control. Only commit this .env.example file.
#
# VARIABLE DESCRIPTIONS:
# APP_NAME        - The name of your application.
# DB_URL          - The MongoDB connection string for your environment.
# LOG_LEVEL       - Logging level (e.g., debug, info, warn, error).
# ENCRYPTION_KEY  - A strong secret key for encryption (must be kept private!).
# PORT            - The port your app will run on.


APP_NAME=PrivacyCompliance
DB_URL=your_mongodb_url_here
LOG_LEVEL=debug
ENCRYPTION_KEY=your_secret_key_here
PORT=4000